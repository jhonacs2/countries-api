import { GetFirstAttrLanguagesPipe } from './get-first-attr-languages.pipe';

xdescribe('GetFirstAttrLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirstAttrLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
